import useTitle from "../../hooks/usTitle";


const AddAToy = () => {
    useTitle('K3 || Add A Toy')
    return (
        <div>
            <h3>Add A Toy Page</h3>
        </div>
    );
};

export default AddAToy;